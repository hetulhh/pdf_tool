services:
  - type: web
    name: pdf-to-excel
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:app --bind 0.0.0.0:10000 --timeout 120 --workers 2
    envVars:
      - key: PDF_API_KEY
        sync: false  # Set manually in Render dashboard
